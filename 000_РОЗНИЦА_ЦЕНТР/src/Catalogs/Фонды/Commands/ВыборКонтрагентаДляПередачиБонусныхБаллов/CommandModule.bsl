&НаКлиенте	//	LNK 27.09.2023 07:50:17
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	Если УправлениеПользователями.ПолучитьБулевоЗначениеПраваПользователя(ПредопределенноеЗначение("ПланВидовХарактеристик.ПраваПользователей.РазрешитьПередачуБонусныхБалловНаБлаготворительныйФонд"), Ложь) Тогда

		ПараметрыФормы = Новый Структура(
			"Заголовок, Выполнить"
			, "Визначення покупця за номером телефону"
			, "Перейти до передачі бонусних балів"
		);

		ОткрытьФорму("Обработка.ДанныеПокупателя.Форма.ВыборКонтрагента"
			, ПараметрыФормы
			, ПараметрыВыполненияКоманды.Источник
			, ПараметрыВыполненияКоманды.Уникальность
			, ПараметрыВыполненияКоманды.Окно
			, ПараметрыВыполненияКоманды.НавигационнаяСсылка
			,
			, РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс
		);

	Иначе

		ПоказатьОповещениеПользователя("Заборонено!"
			,
			, "Користувачеві не надано дозвіл на цю операцію"
			, БиблиотекаКартинок.Ошибка32
		);

	КонецЕсли;

КонецПроцедуры
