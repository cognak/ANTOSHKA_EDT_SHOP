
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("ТипСвойства") Тогда
		
		ОтборыСписковКлиентСервер.УстановитьЭлементОтбораСписка(Список, "ТипСвойства", Параметры.ТипСвойства);			
		Автозаголовок = Ложь;
		Заголовок = НСтр("ru = '" + СокрЛП(Параметры.ТипСвойства) + "'");
		
	КонецЕсли;


КонецПроцедуры
