
&НаКлиенте
Процедура ПолучитьСообщения(Команда)
	ПолучитьСообщенияНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПолучитьСообщенияНаСервере()
	КафкаВызовСервера.ПолучитьСообщение(Объект.Ссылка);
КонецПроцедуры
