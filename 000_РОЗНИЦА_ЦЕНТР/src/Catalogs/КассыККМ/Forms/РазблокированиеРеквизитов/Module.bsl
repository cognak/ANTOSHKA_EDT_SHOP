
///////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	РазрешитьРедактированиеМагазина		= Истина;
	РазрешитьРедактированиеОрганизации	= Истина;
	РазрешитьРедактированиеПРРО			= Истина;
	
КонецПроцедуры

///////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ КОМАНД ФОРМЫ

// Процедура вызывается по кнопке "Разрешить редактирование".
//
&НаКлиенте
Процедура РазрешитьРедактирование(Команда)

	Результат = Новый Массив;
	
	Если РазрешитьРедактированиеМагазина Тогда
		Результат.Добавить("Магазин");
	КонецЕсли;
	Если РазрешитьРедактированиеОрганизации Тогда
		Результат.Добавить("Владелец");
	КонецЕсли;
	Если РазрешитьРедактированиеПРРО Тогда
		Результат.Добавить("ПРРО");
	КонецЕсли;
	
	Закрыть(Результат);

КонецПроцедуры

///////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ
