
Процедура ПередЗаписью(Отказ)

	Если ОбменДанными.Загрузка Тогда

		Возврат;

	КонецЕсли;

	Если НЕ (ПараметрыСеанса.ИсключительныйРежим И ОбменДаннымиПовтИсп.ЭтоГлавныйУзел()) Тогда

		Отказ = Истина;
		Сообщить("Изменение элементов ограничено! Отказано.");

	КонецЕсли;

КонецПроцедуры
