//	LNK 31.01.2017 13:10:49
Функция ПолучитьАдресаСетевыхИнтерфейсов()	Экспорт

	Возврат ТехническаяПоддержка.ПолучитьАдресаСетевыхИнтерфейсов();

КонецФункции // ПолучитьАдресаСетевыхИнтерфейсов()

#Область ПолучениеФлаговРежимаРаботыСеанса

//	LNK 09.02.2017 14:16:30
Функция ИсключительныйРежим()	Экспорт

	ИсключительныйРежим = ПараметрыСеанса.ИсключительныйРежим
					ИЛИ   ПараметрыСеанса.ВыполняютсяСлужебныеДействия
					ИЛИ   ТехническаяПоддержкаПовтИсп.ПолучитьРежимФункционала("Отладка");

	Если НЕ ИсключительныйРежим Тогда

		ИсключительныйРежим = ТехническаяПоддержкаПовтИсп.ПолучитьРежимФункционала("ИсключительныйРежим")
						ИЛИ   ТехническаяПоддержкаПовтИсп.ПолучитьРежимФункционала("ВыполняютсяСлужебныеДействия");

	КонецЕсли;

	Возврат ИсключительныйРежим;

КонецФункции

//	LNK 09.10.2020 04:45:41
Функция ВыполняютсяСлужебныеДействия()	Экспорт

	Если РольДоступна(Метаданные.Роли.АдминистраторСистемы) Тогда

		ВыполняютсяСлужебныеДействия = ПараметрыСеанса.ВыполняютсяСлужебныеДействия
								ИЛИ   ТехническаяПоддержкаПовтИсп.ПолучитьРежимФункционала("Отладка");

		Если НЕ ВыполняютсяСлужебныеДействия Тогда

			ВыполняютсяСлужебныеДействия = ТехническаяПоддержкаПовтИсп.ПолучитьРежимФункционала("ВыполняютсяСлужебныеДействия");

		КонецЕсли;

	Иначе

		ВыполняютсяСлужебныеДействия = Ложь;

	КонецЕсли;

	Возврат ВыполняютсяСлужебныеДействия;

КонецФункции

//	LNK 12.10.2020 07:54:07
Функция ОтладочныйРежимРаботы()	Экспорт

	Возврат ТехническаяПоддержкаПовтИсп.ПолучитьРежимФункционала("Отладка");

КонецФункции

#КонецОбласти

//	LNK 16.01.2018 12:50:52
Функция ТипПлатформыWindows()	Экспорт

	СИ = Новый СистемнаяИнформация;	

	Возврат СИ.ТипПлатформы = ТипПлатформы.Windows_x86 ИЛИ СИ.ТипПлатформы = ТипПлатформы.Windows_x86_64;

КонецФункции // ТипПлатформыWindows()

//	LNK 16.01.2018 12:53:18
Функция ТипПлатформыСистемы()	Экспорт

	СИ = Новый СистемнаяИнформация;	

	Возврат СИ.ТипПлатформы;

КонецФункции // ТипПлатформыСистемы()

//	LNK 16.01.2018 12:54:40
Функция РазделительПути()	Экспорт

	Возврат ?(ТипПлатформыWindows(), "/", "\");

КонецФункции // РазделительПути()
