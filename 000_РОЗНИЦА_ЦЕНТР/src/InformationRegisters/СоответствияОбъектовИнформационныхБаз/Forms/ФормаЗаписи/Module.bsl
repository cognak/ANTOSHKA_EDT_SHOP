&НаКлиенте
Процедура УникальныйИдентификаторИсточникаНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)

	Если НЕ ПустаяСтрока(Запись.ТипИсточника) Тогда

		Элемент.ВыбиратьТип = Ложь;
		Элемент.ОграничениеТипа = Новый ОписаниеТипов(СтрЗаменить(Запись.ТипИсточника, ".", "Ссылка."));

	КонецЕсли;

КонецПроцедуры
