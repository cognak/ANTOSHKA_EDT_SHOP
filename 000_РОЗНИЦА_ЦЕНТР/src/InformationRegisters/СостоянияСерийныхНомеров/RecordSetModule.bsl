Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда

		Возврат;

	КонецЕсли;

	Для каждого ЗаписьНабора Из ЭтотОбъект Цикл

		ЗаписьНабора.ДатаИзменения = ТекущаяДата();

	КонецЦикла;

КонецПроцедуры

//	LNK 21.02.2020 14:58:55
Процедура ПриЗаписи(Отказ, Замещение)

	Если НЕ ОбменДаннымиПовтИсп.ЭтоГлавныйУзел() И ДополнительныеСвойства.Свойство("УведомитьОбИзменении") И НЕ Количество() = 0 Тогда

		ФоновыеЗадания.Выполнить("ТехническаяПоддержка.НазначитьОбъектуДействие", ОбщегоНазначенияКлиентСервер.AAD(Отбор.Регистратор.Значение, Неопределено, "ИЗМЕНЕНИЕ_СОСТОЯНИЯ_ПС", Истина, ПараметрыСеанса.ТекущийПользователь));

	КонецЕсли;

	Если ОбменДанными.Загрузка Тогда

		Возврат;

	КонецЕсли;


КонецПроцедуры
