//	LNK 20.06.2017 11:14:44
Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда

		Возврат;

	КонецЕсли;

КонецПроцедуры

Процедура ПриЗаписи(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда

		Возврат;

	КонецЕсли;

	Если НЕ Количество() = 0 Тогда	//	LNK 04.05.2024 16:05:37

		ВыполнитьРегистрацию = Ложь;

		Для каждого ЗаписьНабора Из ЭтотОбъект Цикл

			Если ВнешниеИсточникиПовтИсп.НужнаРегистрацияПоСверкеNAV(ЗаписьНабора.РезультатСверки)
			Тогда

				ВыполнитьРегистрацию = Истина;
				Прервать;

			КонецЕсли;

		КонецЦикла;

		Если ВыполнитьРегистрацию = Истина
		И НЕ ПланыОбмена.ИзменениеЗарегистрировано(ВнешниеИсточникиПовтИсп.СписокУзловNavision()[0], ЭтотОбъект)
		Тогда

			ПланыОбмена.ЗарегистрироватьИзменения(ВнешниеИсточникиПовтИсп.СписокУзловNavision(), ЭтотОбъект);

		КонецЕсли;

	КонецЕсли;

КонецПроцедуры
