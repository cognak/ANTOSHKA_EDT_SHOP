//	LNK 23.08.2019 14:51:49
Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда

		Возврат;

	КонецЕсли;

	Если Отбор.Магазин.Значение.Пустая() Тогда

		Отбор.Магазин.Установить(ОбменДаннымиПовтИсп.ПолучитьДанныеУзла().Магазин);

	КонецЕсли;

	Для каждого ЗаписьНабора Из ЭтотОбъект Цикл

		ЗаписьНабора.Магазин       = Отбор.Магазин.Значение;
		ЗаписьНабора.ДатаИзменения = ТекущаяДата();

	КонецЦикла;

КонецПроцедуры
