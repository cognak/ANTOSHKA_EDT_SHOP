////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	ПроверкаЗаполнения(Отказ);
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ КОМАНД ФОРМЫ

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

&НаКлиенте
Процедура ПроверкаЗаполнения(Отказ)

	Если НаборКонстант.НижняяГраницаДиапазонаSKUВесовогоТовара > НаборКонстант.ВерхняяГраницаДиапазонаSKUВесовогоТовара Тогда
		
		Отказ = Истина;
		
		ТекстСообщения = НСтр("ru = 'Нижняя граница не может быть больше верхней.'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения, ,"НаборКонстант.НижняяГраницаДиапазонаSKUВесовогоТовара");
		
	КонецЕсли;

КонецПроцедуры


