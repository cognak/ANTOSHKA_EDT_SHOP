&НаСервере	//	LNK 27.09.2019 09:49:39
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
//	Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда

		Возврат;

	КонецЕсли;

	ЭтотОбъект().Журнал(ИмяФормы + ".ПриСозданииНаСервере");	//	LNK 02.08.2019 10:56:00

	Если Параметры.Свойство("АдресТаблицыСкидочныхКупонов") Тогда

		Объект.ПогашениеСкидочныхКупонов.Загрузить(ПолучитьИзВременногоХранилища(Параметры.АдресТаблицыСкидочныхКупонов));

	КонецЕсли;

КонецПроцедуры

&НаСервере	//	LNK 27.09.2019 09:49:21
Функция ЭтотОбъект(ТекущийОбъект = Неопределено) 

	Если ТекущийОбъект = Неопределено Тогда

		Возврат РеквизитФормыВЗначение("Объект");

	КонецЕсли;

	ЗначениеВРеквизитФормы(ТекущийОбъект, "Объект");

	Возврат Неопределено;

КонецФункции

&НаКлиенте	//	LNK 27.09.2019 10:34:56
Процедура ПрименитьДанные(Команда)

	ПараметрыЗакрытия = Новый Структура(
		"АдресТаблицыСкидочныхКупонов", АдресТаблицыСкидочныхКупонов());

	Закрыть(ПараметрыЗакрытия);

КонецПроцедуры

&НаСервере	//	LNK 27.09.2019 10:59:14
Функция АдресТаблицыСкидочныхКупонов()

	Возврат ПоместитьВоВременноеХранилище(Объект.ПогашениеСкидочныхКупонов.Выгрузить());

КонецФункции
