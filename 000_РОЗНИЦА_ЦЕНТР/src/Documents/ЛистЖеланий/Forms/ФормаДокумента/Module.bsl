
&НаСерверебезКонтекста
Функция  ПолучитьСрокДействия(Анкета)
	Возврат ОбщегоНазначения.ПолучитьЗначениеРеквизита(Анкета, "ДатаПразднования");
КонецФункции

&НаКлиенте
Процедура АнкетаПриИзменении(Элемент)
	Объект.СрокДействия = ПолучитьСрокДействия(Объект.Анкета);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	УправлениеДоступомРТ.ПриСозданииФормыНаСервере(ЭтотОбъект);	//	LNK 17.10.2019 14:30:01

КонецПроцедуры
