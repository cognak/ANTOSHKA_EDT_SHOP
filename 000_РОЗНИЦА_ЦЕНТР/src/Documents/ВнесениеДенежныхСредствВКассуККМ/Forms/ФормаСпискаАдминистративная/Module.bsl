&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Если НЕ ТехническаяПоддержкаВызовСервера.ИсключительныйРежим() Тогда

		Отказ = Истина;
		Сообщить("В доступе отказано!");

	КонецЕсли;

КонецПроцедуры
