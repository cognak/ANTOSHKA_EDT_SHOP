
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Путь = ПолучитьПутьСтикера(ПараметрКоманды);
	ПопытКа 
		ЗапуститьПриложение(Путь);
	Исключение
		Сообщить(ОписаниеОшибки())
	КонецПопытки;
КонецПроцедуры

&НаСервере
Функция ПолучитьПутьСтикера(ДокСсылка)
	
	Результат = "";
	ДанныеЭН = Документы.ЭлектроннаяНакладная.ПолучитьЭНПоДокументуОснования(ДокСсылка);
	Если ДанныеЭН.ЕстьТТН Тогда
		Результат = ДанныеЭН.ПутьКФайлуСтикера;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции