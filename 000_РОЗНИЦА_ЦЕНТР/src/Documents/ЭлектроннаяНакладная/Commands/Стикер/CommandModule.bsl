
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	Для Каждого СтрокаПечати Из ПараметрКоманды Цикл
		
		Путь = ПолучитьПутьСтикера(СтрокаПечати);

		Попытка
			
			ЗапуститьПриложение(Путь);
		
		Исключение
		
			Сообщить(ОписаниеОшибки())
		
		КонецПопытки;
		
	КонецЦикла;

КонецПроцедуры

&НаСервере
Функция ПолучитьПутьСтикера(ДокСсылка)
	
	Возврат ДокСсылка.ПутьКФайлуСтикера;

КонецФункции